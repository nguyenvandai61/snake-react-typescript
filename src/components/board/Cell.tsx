interface CellProps {
    state: number
}

export function Cell(props: CellProps) {
    const displayChar = () => {
        switch (props.state) {
            
        }
    }
    return <>
        Cell
    </>
}